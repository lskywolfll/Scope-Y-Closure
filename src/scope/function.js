const fruits = () => {
    var fruit = 'apple';
    console.log(fruit);
};

fruits();

const anotherFunction = () => {
    var numero = 1;
    var numero = 2;
    let numero_dos = 1;
    let numero_dos = 1;

    console.log(numero);
    console.log(numero_dos);
};

anotherFunction();

// Se debe dejar de usar var ya que esto provoca riesgos a la hora de desarrolar tambien producir mala practica, por que no sabremos cuando estamo sobreescribiendo variables que tengamos definidas provocando un error grave.

